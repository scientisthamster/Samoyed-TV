<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SamoyedTV" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/search_background</item>
        <item name="colorPrimaryVariant">@color/search_background</item>
        <item name="colorAccent">@color/search_background</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/bottom_nav_background</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="SplashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Hide the status bar. -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="SearchMovie" parent="Widget.AppCompat.SearchView">
        <item name="tint">@color/white</item>
    </style>

    <style name="MovieButton" parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/GreenButtonThemeOverlay</item>
    </style>

    <style name="GreenButtonThemeOverlay">
        <item name="colorPrimary">@color/search_background</item>
    </style>

</resources>